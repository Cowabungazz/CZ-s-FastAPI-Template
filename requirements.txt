# =========================
# Runtime
# =========================
fastapi==0.112.2
fastapi-utils==0.7.0       # for InferringRouter/cbv
uvicorn[standard]==0.30.5
pydantic>=2.9.2,<3
pydantic-settings>=2.4.0,<3
python-dotenv==1.0.1
httpx==0.27.0
orjson==3.10.7                # optional: faster JSON (FastAPI can auto-detect)
loguru==0.7.2                 # optional: logging
requests==2.32.3              # used in Docker healthcheck or misc utilities
# …(rest unchanged, including the pandas/numpy markers we added)…


# --- Database drivers (enable what you use) ---
# oracledb==2.2.1             # Oracle (cx_Oracle successor)
# asyncpg==0.29.0             # PostgreSQL (async)
# psycopg[binary]==3.2.1      # PostgreSQL (sync), prebuilt wheels
# redis==5.0.8                # Redis (sync)
# aioredis==2.0.1             # Redis (async), if you prefer

# =========================
# Data libs (optional)
# Use wheels that exist across Python versions
# =========================
pandas==2.2.2; python_version < "3.13"
pandas>=2.2.3,<3; python_version >= "3.13"

numpy==2.0.1; python_version < "3.13"
numpy>=2.1.0,<3; python_version >= "3.13"

# =========================
# Testing
# =========================
pytest==8.3.2
pytest-cov==5.0.0

# =========================
# Lint / Format / Type checking (dev)
# =========================
ruff==0.5.7
black==24.8.0
mypy==1.11.1
types-requests==2.32.0.20240712
